# Зависимости
node_modules/

# Сборка (продакшен-версия)
dist/
.vite/

# Среда разработки
.env.local
.env.development.local
.env.test.local
.env.production.local

# ОС
.DS_Store
Thumbs.db

# IDE / редакторы
.idea/
.vscode/
*.swp
*.swo

# Логи и артефакты
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Локальные данные (твой браузерный localStorage в DevTools — не сохраняется, но на всякий случай)
# (на самом деле localStorage не сохраняется в проекте, но если вдруг создашь файлы данных — они не попадут)